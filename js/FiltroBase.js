class FiltroBase{
    constructor(ctx, canvas){
        this.ctx = ctx;
        this.canvas = canvas;
        this.width = canvas.width;
        this.height = canvas.height;
    }
    // setPixel(data, x, y, r, g, b, width){};
}